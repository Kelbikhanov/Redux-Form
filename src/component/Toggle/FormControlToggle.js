import styles from "../styles/FormsControl.module.css";
import React from "react";

export const ToggleFC = (props) => {
    return (
        <input className={styles.checkbox} type={"checkbox"} />
    )
};